[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jmd-format"
version = "0.6.2"
description = "JMD (JSON Markdown) — structured data format for LLM-driven infrastructure"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Andreas Ostermeyer", email = "andreas@ostermeyer.de" }]
requires-python = ">=3.10"
dependencies = []
keywords = ["jmd", "llm", "structured-data", "json", "markdown", "mcp"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Markup",
]

[project.optional-dependencies]
# XML companion (jmd.xml) — only required for the JMD-over-XML mapping.
# Install with: pip install "jmd-format[xml]"
xml = ["lxml>=5.0"]

[project.urls]
Homepage = "https://github.com/ostermeyer/jmd-spec"
Repository = "https://github.com/ostermeyer/jmd-impl"

[project.scripts]
jmd = "jmd._cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["jmd*"]

[tool.setuptools.package-data]
jmd = ["py.typed", "*.c"]

[tool.ruff]
line-length = 80
target-version = "py310"
# vendor/jmd-spec is a git submodule (external spec checkout +
# its benchmark harness) — never lint third-party vendored code.
extend-exclude = ["vendor"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "D",   # pydocstyle (Google convention)
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
strict = true

# Pytest 9's stubs ship `@pytest.fixture` and `@pytest.mark.*` as
# `Callable[..., Any]`; under `strict = true` that trips
# `untyped-decorator` on every fixture / parametrize wrapper, even
# though the wrapped functions themselves are fully annotated. The
# tests don't ship as a public surface, so we silence the noise
# rather than annotate each decoration site.
[[tool.mypy.overrides]]
module = "tests.*"
disable_error_code = ["untyped-decorator"]

# lxml-stubs may not be installed on every developer's machine (they are a
# dev-only dep and CI may omit them). When absent, mypy treats lxml as
# untyped, which makes all the type: ignore[arg-type] comments in xml.py
# appear "unused". Silencing the import-untyped error keeps the gate green
# on every platform without removing the legitimate arg-type suppressions.
[[tool.mypy.overrides]]
module = "lxml.*"
ignore_missing_imports = true

[dependency-groups]
dev = [
    "hypothesis>=6.0",
    "lxml-stubs>=0.5.1",
    "mypy>=1.20.1",
    "pytest>=8.0",
    "ruff>=0.15.10",
]
